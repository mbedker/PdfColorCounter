# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET         /                                                   controllers.Application.index()

#PDF Endpoints
POST       /pdf/start                                          controllers.Application.countPDF()
GET        /pdf/:pdfSessionId/:pageNumber                      controllers.Application.pageInformation(pdfSessionId, pageNumber)
GET        /pdf/:pdfSessionId                                  controllers.Application.status(pdfSessionId)
GET        /pdf/image/:pdfSessionId/:pageNumber                controllers.Application.pageImage(pdfSessionId, pageNumber)

# Map static resources from the /public folder to the /assets URL path
GET         /assets/*file                                       controllers.Assets.at(path="/public", file)
